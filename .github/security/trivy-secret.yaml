# Custom Trivy secret-scanning rules (adds to the built-in rules).
# The org-name pattern is written as n[a]acp so this file doesn't match itself.
rules:
  - id: org-name-reference
    category: general
    title: Organization name reference (must stay out of this repo)
    severity: HIGH
    regex: (?i)n[a]acp
